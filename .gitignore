# Python
pipeline/.venv/
__pycache__/
*.py[cod]

# --collect-only が吐く生データ。中身の確認用でリポジトリには入れない。
pipeline/collected.json

# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
*.iml
*.apk

# OS / editor
.DS_Store
Thumbs.db

# このマシン固有の Claude Code 設定。操作のたびに書き換わるので追跡しない。
.claude/settings.local.json
